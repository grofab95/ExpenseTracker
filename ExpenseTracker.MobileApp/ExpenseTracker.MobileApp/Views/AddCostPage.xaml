<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ExpenseTracker.MobileApp.Views.AddCostPage">
    
    <!--<ContentPage.ToolbarItems>
        <ToolbarItem Text="Anuluj" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Zapisz" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>-->

    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Nazwa" FontSize="Medium" />
            <Entry Text="{Binding Item.Text}" d:Text="Item name" FontSize="Small" />
            <Label Text="Kategoria" FontSize="Medium" />
            <Entry Text="{Binding Item.Text}" d:Text="Item name" FontSize="Small" />
            <Label Text="Opis" FontSize="Medium" />
            <Editor Text="{Binding Item.Description}" AutoSize="TextChanges"
                    d:Text="Item description" FontSize="Small" Margin="0" />

            <StackLayout Spacing="8">
                <Button 
                    Text="Dodaj Wydatek" Visual="Material"
                    x:Name="AddCostButton" Clicked="AddCostButton_Clicked"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>